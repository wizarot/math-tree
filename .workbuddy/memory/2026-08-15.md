# 2026-08-15 工作日志

## README 链接与授权修正
- 移除「授权相关」内容：顶部 License 徽章、末尾「📄 开源协议」章节（项目暂未设许可证）。
- 所有链接统一改为仓库 `wizarot/math-tree`：
  - GitHub Pages 访问页：`https://wizarot.github.io/math-tree`
  - 仓库地址：`https://github.com/wizarot/math-tree`
  - 贡献指南 clone 地址 + `cd math-tree`、项目结构根目录标签、访问计数 username 同步更新。
- 提交 `7495a18 docs: README 链接改为 math-tree 仓库，移除许可证章节`，并成功 `git push -u origin master`（a0624e6..7495a18）。

## 备注
- 6 张截图（1~6.png）已于早前提交 `a0624e6 增加一些图片` 入库。
- 远程 `origin` 指向 `git@github.com:wizarot/math-tree.git`（SSH）。

## 页面新增 GitHub 入口
- `index.html` 与 `index.ordered.html` 顶部按钮栏新增 GitHub 图标链接按钮（内联 octocat SVG，复用 `.gbtn`/`.hbtn` 样式），指向 `https://github.com/wizarot/math-tree`，新标签页打开。
- 给有序页 `.hbtn` 补 `display:inline-flex;align-items:center;gap:6px` 保证图标文字对齐。
- 提交 `91b25ba feat: 顶部 HUD 增加 GitHub 图标链接，指向项目仓库`（未 push，待用户手动推送）。

## 引流发布素材
- 新增 `发布素材.md`：汇总 GitHub 仓库元数据（description/topics/homepage 复制即用）+ 中文平台（V2EX/知乎/掘金/少数派）文案 + 英文 Show HN（Hacker News/Reddit）文案 + 各平台发帖入口 URL + 发布顺序建议。
- 用途：用户自行去各平台发帖引流；本次未 push，待用户手动推送。
